<div class="map icon" data-id="{{$map->id}}">
    <div class="content">
        <img src="{{asset($map->image)}}" alt="{{$map->name}}">
        <div class="tooltip-description">
            <h4>{{$map->name}}</h4>
        </div>
    </div>
</div>