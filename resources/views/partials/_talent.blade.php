<div class="talent" data-id="{{$talent->id}}">
    <img src="{{$talent->image}}" alt="{{$talent->name}}">
    <div class="tooltip-description">
        <p>
            {{$talent->description}}
        </p>
    </div>
</div>